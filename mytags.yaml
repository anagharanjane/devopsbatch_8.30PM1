---
- hosts:
  become: yes
  tasks:
   -name: i want to install tree
    apt:

  -name: i want to install wget
   apt:
     name: wget
     state: present
     tags: mywget

  -name: i want to install git
   apt: 
      name: git
      state: present
      tags: mygit